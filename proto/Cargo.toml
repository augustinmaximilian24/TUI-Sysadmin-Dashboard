[package]
name = "logsentry-proto"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
# Für async Framing (proto::framing), das Daemon UND Client gleichermaßen
# nutzen -- daher keine optionale Abhängigkeit hinter einem Feature.
tokio.workspace = true

[features]
# Reserviert für proto::client (Schritt 3): Reconnect-Task, der Daemon
# selbst nicht braucht. tokio ist ab jetzt ohnehin immer vorhanden; dieses
# Feature schaltet künftig nur das Modul frei, keine zusätzliche Dependency.
client = []
